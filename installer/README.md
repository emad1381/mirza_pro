# راهنمای نصب ربات میرزا پرو

## 🚀 نصب سریع

### پیش‌نیازها

قبل از شروع نصب، مطمئن شوید که:

1. **سرور لینوکس** با Ubuntu 20.04 یا بالاتر
2. **PHP 8.2+** نصب شده باشد
3. **MySQL/MariaDB** نصب و راه‌اندازی شده باشد
4. **Apache** یا **Nginx** به عنوان وب سرور
5. **SSL Certificate** (HTTPS) روی دامنه فعال باشد
6. **توکن ربات تلگرام** از [@BotFather](https://t.me/BotFather) دریافت کرده باشید

---

## 📦 مراحل نصب

### گام 1: آپلود فایل‌ها

1. تمام فایل‌های پروژه را در پوشه‌ای در سرور آپلود کنید (مثلا `/var/www/html/mirzaprobotconfig`)
2. مطمئن شوید که پوشه `installer` موجود است

### گام 2: دسترسی به نصاب

در مرورگر خود به آدرس زیر بروید:

```
https://yourdomain.com/mirzaprobotconfig/installer/installer.html
```

یا اگر پروژه در ریشه قرار دارد:

```
https://yourdomain.com/installer/installer.html
```

### گام 3: طی کردن مراحل نصب

نصاب شامل 6 مرحله است:

#### مرحله 1: خوش‌آمدگویی
- معرفی سیستم نصب
- راهنمای کلی

#### مرحله 2: بررسی سیستم
نصاب به طور خودکار موارد زیر را بررسی می‌کند:
- ✅ نسخه PHP (8.2+)
- ✅ افزونه‌های ضروری PHP
- ✅ دسترسی نوشتن در پوشه
- ✅ وب سرور فعال

#### مرحله 3: تنظیمات دیتابیس
اطلاعات زیر را وارد کنید:

**دیتابیس جدید (خودکار ساخته می‌شود):**
- نام دیتابیس: `mirzaprobot` (پیشنهادی)
- نام کاربری دیتابیس: دلخواه
- رمز عبور دیتابیس: از دکمه "تولید رمز امن" استفاده کنید

**دسترسی Root:**
- نام کاربری Root: معمولا `root`
- رمز عبور Root: رمز root دیتابیس MySQL

> 💡 **نکته**: نصاب به طور خودکار دیتابیس را می‌سازد و کاربر جدید ایجاد می‌کند.

#### مرحله 4: تنظیمات ربات
اطلاعات زیر را وارد کنید:

- **توکن ربات**: از [@BotFather](https://t.me/BotFather) دریافت کنید
  - فرمت: `123456789:ABCdefGHIjklMNOpqrsTUVwxyz`
- **شناسه عددی ادمین**: Chat ID خود را از [@userinfobot](https://t.me/userinfobot) دریافت کنید
- **دامنه سایت**: بدون `https://` وارد کنید (مثال: `example.com`)
- **نام کاربری ربات**: بدون `@` (مثال: `MyBot`)

#### مرحله 5: نصب
- بررسی نهایی تنظیمات
- کلیک روی دکمه "شروع نصب"
- منتظر بمانید تا فرآیند نصب کامل شود

#### مرحله 6: تکمیل
- مشاهده اطلاعات دسترسی
- کپی کردن اطلاعات مهم
- حذف پوشه installer برای امنیت

---

## 🔑 اطلاعات دسترسی

بعد از نصب موفق، اطلاعات زیر به شما نمایش داده می‌شود:

- **دامنه ربات**: `https://yourdomain.com/mirzaprobotconfig`
- **phpMyAdmin**: `https://yourdomain.com/phpmyadmin`
- **نام دیتابیس**: مثلا `mirzaprobot`
- **کاربر دیتابیس**: یوزرنیمی که تعریف کردید
- **رمز دیتابیس**: رمزی که تولید شد

> ⚠️ **مهم**: حتما این اطلاعات را در جای امنی ذخیره کنید!

---

## 🔒 امنیت

### حذف نصاب

**بلافاصله** بعد از نصب موفق، پوشه `installer` را حذف کنید:

```bash
rm -rf /var/www/html/mirzaprobotconfig/installer
```

یا از دکمه "پایان و حذف نصاب" در مرحله آخر استفاده کنید.

### فایل .installed

نصاب به طور خودکار فایلی به نام `.installed` ایجاد می‌کند که از نصب مجدد جلوگیری می‌کند.

---

## ✅ تست ربات

بعد از نصب:

1. در تلگرام به ربات خود بروید
2. دستور `/start` را ارسال کنید
3. اگر ربات پاسخ داد، نصب موفقیت‌آمیز بوده است! 🎉

---

## 🐛 عیب‌یابی

### ربات پاسخ نمی‌دهد

1. بررسی کنید Webhook به درستی ثبت شده:
```
https://api.telegram.org/bot<TOKEN>/getWebhookInfo
```

2. مطمئن شوید SSL سرور فعال و معتبر است

3. لاگ‌های Apache را بررسی کنید:
```bash
tail -f /var/log/apache2/error.log
```

### خطای دیتابیس

1. بررسی کنید اطلاعات `config.php` صحیح است
2. دسترسی‌های کاربر دیتابیس را چک کنید
3. مطمئن شوید MySQL در حال اجراست:
```bash
systemctl status mysql
```

### خطای دسترسی فایل‌ها

مجوزهای فایل را تنظیم کنید:
```bash
chown -R www-data:www-data /var/www/html/mirzaprobotconfig
chmod -R 755 /var/www/html/mirzaprobotconfig
```

---

## 📞 پشتیبانی

- **کانال تلگرام**: [@mirzapanel](https://t.me/mirzapanel)
- **گروه تلگرام**: [@mirzapanelgroup](https://t.me/mirzapanelgroup)
- **مخزن گیت‌هاب**: [mahdiMGF2/mirza_pro](https://github.com/mahdiMGF2/mirza_pro)

---

## 📝 توجه

- این نصاب فقط برای نصب اولیه طراحی شده است
- برای ارتقاء، از دستورات مخصوص استفاده کنید
- همیشه قبل از تغییرات مهم، بک‌آپ بگیرید

---

**ساخته شده با ❤️ برای جامعه اوپن‌سورس**
